name: First workflow
on: 
  push:
  issues:
  pull_request:
    types: 
      - opened
      - syncrhonized
      - labeled
jobs:
  show-labels:
    runs-on: ubuntu-latest
    steps:
      - name: Show Labels
        run: echo '${{ toJson(github) }}'
