name: First workflow
on: 
  push:
  pull_request:
    types: 
      - opened
      - syncrhonized
jobs:
  show-labels:
    runs-on: ubuntu-latest
    steps:
      - name: Show Labels
        run: echo '${{ toJson(github) }}'
